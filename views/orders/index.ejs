<%- include('../_partial/header.ejs') %>
    <table id="example" class="table table-striped table-bordered table-sm" cellspacing="0" width="100%">
        <thead>
            <tr>
                <th>Name</th>
                <th>status</th>
                <th>Order date</th>
                <th>Products</th>
            </tr>
        </thead>
        <tbody>
            <% data.forEach(e => { %>
                <tr>
                    <td><%= e.Customer.getFullName() %> </td>
                    <td><%= e.status %></td>
                    <td><%= e.getDate() %></td>
                    <td><%= e.Products[0].name %> </td>
                </tr>
            <% }) %>  
        </tbody>
    </table>

<%- include('../_partial/footer.ejs') %> 